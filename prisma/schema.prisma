// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// enum SongStatus {
//   STORED
//   GOST
// }

model Song {
  id            Int     @id @default(autoincrement())
  title         String
  artists       String
  album         String
  genre         String
  label         String
  localy_stored Boolean @default(false)
  url           String?
  path          String?
}
